name: agencia_empleo_udec
description: Agencia de Empleo - Universidad de Cundinamarca
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.6.0
  file_picker: ^8.0.0
  shared_preferences: ^2.2.3
  cupertino_icons: ^1.0.6
  image_picker: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true